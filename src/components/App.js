import '../styles/App.css';
import React from 'react';
import Layout from './Layout';

const App = () => {
  return (
    <Layout>
      I am the children of Layout component
    </Layout>
  );
};

export default App;